# MAIN experiment on the Olmo-3.1-32B 's1_328k' training snapshot (Experiment C —
# when during training does controllability appear). Loaded from the LOCAL dir
# /ckpts/olmo31_32b_s1_328k (see scripts/olmo_snapshot_lane.sh for the repo/revision it
# must contain; the lane downloads it and wipes it after the run).
# Raw activations for snapshot runs are PRUNED after postprocess — JSONs only.
# Inherits everything from config.yaml.
extends: config.yaml

model:
  name: "olmo31_32b_s1_328k"
  quantization: null
