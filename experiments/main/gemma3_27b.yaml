# MAIN experiment on Gemma-3-27B-it (the focal model of the paper). Inherits
# everything from config.yaml; only the model changes, so it runs the SAME
# standardized protocol as the cross-model battery (50 sentences, 10 concepts,
# 19 conditions, depth fractions resolved to gemma3-27b's 62 layers).
extends: config.yaml

model:
  name: "gemma3_27b"
